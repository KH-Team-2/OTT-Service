SELECT * FROM SP_CONTENTS;
SELECT * FROM SP_PLATFORM;
SELECT * FROM REVIEW;
SELECT * FROM USERTB;
SELECT * FROM WISHLIST;
SELECT * FROM NOTIFICATION;

select * from user_sequences;
                  
SELECT MAX(MovieNum) FROM SP_PLATFORM WHERE PFCODE IN 'WC';
SELECT MovieNum FROM SP_CONTENTS SC WHERE SC.MovieNum > 2;

SELECT COUNT(*) FROM USERTB WHERE ID = 'RT' AND NAME = 'hello' AND EMAIL = 'wnehdwlswn@naver.com';

DELETE FROM USERTB WHERE NAME = '주동진';
SELECT * FROM USERTB WHERE NAME = '주동진';

INSERT INTO NOTIFICATION VALUES ( ALARM_SQ.NEXTVAL, 34, '알람 내용4', 'F', SYSDATE );

SELECT CONSTRAINT_NAME, STATUS
FROM ALL_CONSTRAINTS
WHERE TABLE_NAME='NOFITICATION';

SELECT * FROM    ALL_CONSTRAINTS
WHERE    TABLE_NAME = 'NOFITICATION';

UPDATE WISHLIST 
	SET ALARM = 
		CASE
			WHEN ALARM='Y' THEN 'N'
			WHEN ALARM='N' THEN 'Y'
		END
WHERE USERNUM = ? AND MOVIENUM = ?

UPDATE SP_WISHLIST 
SET ALARM = 
CASE 
WHEN ALARM='Y' THEN 'N' 
WHEN ALARM='Y' THEM 'N' 
WHERE USERNUM=? AND MOVIENUM=?;

UPDATE WISHLIST
SET ALARM = 
	( CASE
		WHEN ALARM='Y' THEN 'N'
		WHEN ALARM='N' THEN 'Y'
	END )
WHERE USERNUM=1 AND MOVIENUM=1;
